(def in (pipes/debug))
(def out (pipes/log "logger"))
(| in (inc _) out)
(defmodule logger {:sources {:logger-in in}})
(defmodule use {:depends {:log-use logger} :sources {:use-in in}})
(def logger-in (-> logger :-sources :-logger-in))
(def logger-in-fn (logger-in))
(def tester (pipes/debug))
(| tester logger-in-fn)
!f tester 5
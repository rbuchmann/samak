(def init (pipes/station))
(def in (pipes/station))
(def out (pipes/log "foobar"))
(def counter (pipes/reductions (+ :-state :-next) 0))
(| in (-> (repeat _ 1) many) counter)
(| init (str-to-int (first _)) in)
(| counter out)
